(() => {
  if (window.__BALI_CHIP_REQUESTS_USER__) return;
  window.__BALI_CHIP_REQUESTS_USER__ = true;
  const requests = window.BaliChipRequests;
  const points = window.BaliPoints;
  if (!requests || !points) return;
  const esc=(v="")=>String(v).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
  const fmt=v=>v?new Date(v).toLocaleString("ru-RU",{day:"2-digit",month:"short",year:"numeric",hour:"2-digit",minute:"2-digit"}):"—";
  const toast=m=>{const e=document.getElementById("toast");if(!e)return;e.textContent=m;e.classList.add("show");clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove("show"),2400)};
  function styles(){if(document.getElementById("chipRequestUserStyle"))return;const s=document.createElement("style");s.id="chipRequestUserStyle";s.textContent=`.chip-request-box{display:grid;gap:11px;padding:14px;border:1px solid var(--line);border-radius:17px;background:#ffffff06}.chip-request-box label{display:grid;gap:6px;color:var(--muted);font-size:9px;font-weight:800}.chip-request-box input{width:100%;min-height:48px;padding:0 12px;border:1px solid var(--line);border-radius:13px;background:#171b19;color:#fff}.chip-request-cost{display:flex;justify-content:space-between;align-items:center;gap:10px;padding:12px;border:1px solid rgba(200,255,61,.2);border-radius:13px;background:rgba(200,255,61,.06)}.chip-request-cost span{color:var(--muted);font-size:9px}.chip-request-cost strong{color:var(--lime);font:600 18px Unbounded}.chip-request-note{margin:0;color:var(--muted);font-size:8px;line-height:1.55}.chip-request-history{display:grid;gap:7px;margin-top:10px}.chip-request-row{display:grid;grid-template-columns:1fr auto;gap:8px;padding:10px;border:1px solid var(--line);border-radius:13px;background:#ffffff05}.chip-request-row strong{font-size:10px}.chip-request-row small{display:block;margin-top:3px;color:var(--muted);font-size:8px}.chip-request-status{align-self:center;padding:5px 7px;border-radius:999px;font-size:8px;font-weight:900}.chip-request-status.pending{background:#f3c75d1c;color:#f3c75d}.chip-request-status.fulfilled{background:#c8ff3d18;color:var(--lime)}.chip-request-status.cancelled{background:#ff73731a;color:#ff9696}`;document.head.appendChild(s)}
  function cleanBalance(){const balance=document.querySelector("#profilePointsBody .profile-v2-balance small");if(balance)balance.textContent="BALI-Баллов"}
  async function render(){const root=document.getElementById("profilePointsBody");if(!root)return;cleanBalance();const heading=[...root.querySelectorAll("h3")].find(h=>/Обмен баллов на фишки/i.test(h.textContent||""));if(!heading)return;const section=heading.closest("section")||heading.parentElement;if(!section||section.dataset.chipRequestReady==="1")return;section.dataset.chipRequestReady="1";const rate=requests.rate(),mine=requests.mine(await requests.list()).slice(0,8);section.innerHTML=`<h3>Приобрести фишки</h3><div class="chip-request-box"><label><span>Количество фишек</span><input id="chipRequestQuantity" type="number" min="1" step="1" value="1"></label><div class="chip-request-cost"><span>Стоимость</span><strong id="chipRequestCost">${rate} баллов</strong></div><button class="primary full" id="chipRequestSubmit" type="button">Приобрести фишки</button><p class="chip-request-note">После оформления заявка поступит администратору. Фишки выдаются лично в заведении. Отдельный цифровой баланс фишек в профиле не ведётся.</p></div><div class="chip-request-history">${mine.map(row=>`<article class="chip-request-row"><div><strong>${Number(row.quantity||0)} фиш. · ${Number(row.points_cost||0)} баллов</strong><small>${fmt(row.created_at)}</small></div><span class="chip-request-status ${esc(row.status||"pending")}">${row.status==="fulfilled"?"Вручено":row.status==="cancelled"?"Отменено":"Ожидает"}</span></article>`).join("")}</div>`;const input=section.querySelector("#chipRequestQuantity"),cost=section.querySelector("#chipRequestCost");const update=()=>{const q=Math.max(1,Math.floor(Number(input.value||1)));cost.textContent=`${q*rate} баллов`};input.addEventListener("input",update);section.querySelector("#chipRequestSubmit").addEventListener("click",async()=>{const q=Math.max(1,Math.floor(Number(input.value||0))),total=q*rate;if(!confirm(`Оформить заявку на ${q} фиш. за ${total} BALI-Баллов?`))return;const result=await requests.create(q);toast(result.ok?"Заявка отправлена администратору":result.message);if(result.ok){section.dataset.chipRequestReady="0";render()}})}
  styles();document.addEventListener("click",e=>{if(e.target.closest("[data-open-profile-points]"))setTimeout(render,0)},true);let scheduled=false;new MutationObserver(records=>{if(scheduled||!records.some(r=>r.addedNodes.length))return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;render()})}).observe(document.body,{childList:true,subtree:true});window.addEventListener("bali:chip-requests-changed",()=>setTimeout(render,0));
})();