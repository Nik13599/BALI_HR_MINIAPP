(() => {
  const keys = {
    settings: "bali_bonus_settings_v1",
    profile: "bali_bonus_profile_v1",
    ledger: "bali_bonus_ledger_v1",
    actions: "bali_bonus_actions_v1",
    visits: "bali_attendance_codes_v1",
    accounts: "bali_points_accounts_v1"
  };
  const defaults = { referral: 50, attendance: 100, eventShare: 10, review: 100 };
  const read = (key, fallback) => { try { return JSON.parse(localStorage.getItem(key)) ?? fallback; } catch { return fallback; } };
  const write = (key, value) => { localStorage.setItem(key, JSON.stringify(value)); window.dispatchEvent(new CustomEvent("bali:points-changed", { detail: { key } })); return value; };
  const normalizePhone = (value = "") => String(value).replace(/\D/g, "");
  const settings = () => { const value = read(keys.settings, {}); return { ...defaults, ...value, attendance: Number(value.attendance ?? value.story ?? defaults.attendance), review: Number(value.review ?? defaults.review) }; };
  const accounts = () => read(keys.accounts, {});
  const accountKey = (data = {}) => data.userKey || data.ownerKey || (data.phone ? `phone:${normalizePhone(data.phone)}` : "") || (data.telegramId ? `tg:${data.telegramId}` : "") || data.code || "";
  function saveAccount(account) { const key = accountKey(account); if (!key) return account; const all = accounts(); const previous = all[key] || null; const candidate = { ...(previous || {}), ...account, userKey: key }; const previousComparable = previous ? { ...previous } : null; const candidateComparable = { ...candidate }; if (previousComparable) delete previousComparable.updatedAt; delete candidateComparable.updatedAt; if (previousComparable && JSON.stringify(previousComparable) === JSON.stringify(candidateComparable)) return previous; candidate.updatedAt = new Date().toISOString(); all[key] = candidate; write(keys.accounts, all); return candidate; }
  const profile = () => { const saved = read(keys.profile, null); if (saved?.code) { if (!saved.userKey) { const telegramId = window.Telegram?.WebApp?.initDataUnsafe?.user?.id; saved.userKey = telegramId ? `tg:${telegramId}` : saved.code; write(keys.profile, saved); } const registry = accounts(); if (!registry[saved.userKey]) saveAccount(saved); return saved; } const user = window.Telegram?.WebApp?.initDataUnsafe?.user; const source = user?.id ? String(user.id) : String(Date.now()).slice(-7); const created = { code: `BALI-${source.slice(-7).toUpperCase()}`, userKey: user?.id ? `tg:${user.id}` : `BALI-${source.slice(-7).toUpperCase()}`, telegramId: user?.id || null, name: user?.first_name || "Гость BALI", balance: 0, createdAt: new Date().toISOString() }; write(keys.profile, created); saveAccount(created); return created; };
  const ledger = () => read(keys.ledger, []); const actions = () => read(keys.actions, {}); const visits = () => read(keys.visits, []);
  function linkIdentity(data = {}) { const current = profile(); const key = accountKey(data) || current.userKey || current.code; const linked = { ...current, userKey: key, name: data.name || current.name, phone: normalizePhone(data.phone) || current.phone || "", telegram: data.telegram || current.telegram || "", ownerKey: data.ownerKey || current.ownerKey || key }; write(keys.profile, linked); saveAccount(linked); return linked; }
  function add(type, amount, title, actionKey) { const used = actions(); if (actionKey && used[actionKey]) return false; const user = profile(); const value = Math.max(0, Number(amount || 0)); user.balance = Number(user.balance || 0) + value; const rows = ledger(); rows.unshift({ id: crypto.randomUUID?.() || String(Date.now()), userKey: user.userKey, type, title, amount: value, createdAt: new Date().toISOString() }); if (actionKey) used[actionKey] = new Date().toISOString(); write(keys.profile, user); write(keys.ledger, rows.slice(0, 100)); write(keys.actions, used); saveAccount(user); return true; }
  function adjustAccount(target, delta, note = "Корректировка администратора") { const key = accountKey(target); if (!key) return { ok: false, message: "Не выбран пользователь" }; const all = accounts(); const currentProfile = profile(); const existing = all[key] || { userKey: key, code: target.code || "", name: target.name || "Пользователь BALI", phone: normalizePhone(target.phone), telegram: target.telegram || "", balance: 0, createdAt: new Date().toISOString() }; const value = Number(delta || 0); if (!value) return { ok: false, message: "Укажите количество баллов" }; existing.balance = Math.max(0, Number(existing.balance || 0) + value); existing.updatedAt = new Date().toISOString(); all[key] = existing; write(keys.accounts, all); const rows = ledger(); rows.unshift({ id: crypto.randomUUID?.() || String(Date.now()), userKey: key, type: value > 0 ? "admin_add" : "admin_remove", title: note, amount: value, createdAt: new Date().toISOString() }); write(keys.ledger, rows.slice(0, 200)); if (currentProfile.userKey === key || currentProfile.ownerKey === key || currentProfile.code === key) write(keys.profile, { ...currentProfile, ...existing, userKey: key }); return { ok: true, account: existing, delta: value }; }
  function redeemVisit(rawCode) { const code = String(rawCode || "").trim().toUpperCase(); if (!code) return { ok: false, message: "Введите код посещения" }; const rows = visits(); const index = rows.findIndex((item) => String(item.code || "").toUpperCase() === code); if (index < 0) return { ok: false, message: "Код посещения не найден" }; if (rows[index].usedAt) return { ok: false, message: "Этот код уже использован" }; const amount = Number(rows[index].amount ?? settings().attendance); const title = rows[index].eventTitle ? `Посещение «${rows[index].eventTitle}»` : "Посещение мероприятия BALI"; if (!add("attendance", amount, title, `attendance-${code}`)) return { ok: false, message: "Баллы уже начислены" }; rows[index] = { ...rows[index], usedAt: new Date().toISOString(), usedBy: profile().code }; write(keys.visits, rows); return { ok: true, amount, title }; }
  window.BaliPoints = { keys, defaults, read, write, settings, profile, ledger, actions, visits, accounts, saveAccount, linkIdentity, add, adjustAccount, redeemVisit, accountKey };
})();