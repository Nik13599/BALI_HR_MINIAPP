(() => {
  if (window.BaliBeta4Social || !window.BaliBeta4Game) return;
  const game = window.BaliBeta4Game;
  const KEYS = { profile:"bali_social_profile_v1", people:"bali_social_people_v1", requests:"bali_social_requests_v1", gifts:"bali_social_gifts_v1", giftCatalog:"bali_social_gift_catalog_v1", thumbs:"bali_social_swipes_v2" };
  const STATUSES = [["party","Ищу компанию на вечеринку"],["table","Ищу компанию для бронирования столика"],["chat","Открыт(а) к общению"],["closed","Не знакомлюсь"]];
  const DEFAULT_GIFT_CATALOG = [{id:"rose",icon:"🌹",name:"Роза",stars:25,active:true},{id:"cocktail",icon:"🍸",name:"Коктейль",stars:50,active:true},{id:"disco",icon:"🪩",name:"Диско-шар",stars:100,active:true},{id:"crown",icon:"👑",name:"VIP-корона",stars:250,active:true}];
  const GIFT_CATALOG = DEFAULT_GIFT_CATALOG.map(item=>({...item}));
  const read=(key,fallback)=>{try{return JSON.parse(localStorage.getItem(key))??fallback}catch{return fallback}};
  const write=(key,value)=>{localStorage.setItem(key,JSON.stringify(value));window.dispatchEvent(new CustomEvent("bali:social-changed",{detail:{key}}));return value};
  function normalizeGift(item={},index=0){return{id:String(item.id||`gift-${index+1}`),icon:String(item.icon||"🎁"),name:String(item.name||"Подарок BALI").trim(),stars:Math.max(1,Math.floor(Number(item.stars??item.points??1))),active:item.active!==false,sortOrder:Number(item.sortOrder??index+1)}}
  function syncGiftCatalog(){const saved=read(KEYS.giftCatalog,null),rows=(Array.isArray(saved)&&saved.length?saved:DEFAULT_GIFT_CATALOG).map(normalizeGift).sort((a,b)=>a.sortOrder-b.sortOrder);GIFT_CATALOG.splice(0,GIFT_CATALOG.length,...rows);return GIFT_CATALOG}
  function saveGiftCatalog(rows){const saved=(rows||[]).map(normalizeGift);write(KEYS.giftCatalog,saved);syncGiftCatalog();return GIFT_CATALOG}
  syncGiftCatalog();
  const now=()=>new Date().toISOString(),uid=p=>`${p}-${crypto.randomUUID?.()||Date.now()}`,base=()=>game.profile(),myId=()=>String(base().id||base().userKey||base().code);
  const dateAt=(date,time="00:00")=>{const value=new Date(`${String(date||"").slice(0,10)}T${time||"00:00"}:00`);return Number.isNaN(value.getTime())?null:value};
  function eventEndAt(event={}){const startDate=event.event_date||event.eventDate||"",startTime=event.event_time||event.eventTime||"23:00",endTime=event.event_end_time||event.eventEndTime||event.end_time||"06:00";let endDate=event.event_end_date||event.eventEndDate||event.end_date||startDate;if(!event.event_end_date&&!event.eventEndDate&&!event.end_date&&endTime<=startTime){const date=dateAt(startDate,"12:00");if(date){date.setDate(date.getDate()+1);endDate=date.toISOString().slice(0,10)}}return dateAt(endDate,endTime)?.toISOString()||""}
  function profile(){const p=base(),saved=read(KEYS.profile,{});return{id:myId(),name:p.name||"Гость BALI",username:p.username||"",photo:saved.photo||p.avatar||"",cropX:Number(saved.cropX??50),cropY:Number(saved.cropY??40),status:saved.status||"closed",bio:saved.bio||"",active:saved.active===true,shareTelegram:false,...saved}}
  function cleanPeople(rows){return(rows||[]).filter(item=>item&&item.id&&!String(item.id).startsWith("demo-"))}
  function saveProfile(patch={}){const next={...profile(),...patch,id:myId(),updatedAt:now()};write(KEYS.profile,next);const rows=cleanPeople(read(KEYS.people,[])).filter(x=>x.id!==next.id);rows.unshift(next);write(KEYS.people,rows);return next}
  function people(){const me=profile(),rows=cleanPeople(read(KEYS.people,[]));return[me,...rows.filter(x=>x.id!==me.id)]}
  function visiblePeople(){return people().filter(x=>x.id!==myId()&&x.active===true&&x.status!=="closed")}
  const thumbs=()=>read(KEYS.thumbs,[]);
  function setThumb(fromId,toId,active=true){const rows=thumbs().filter(x=>!(x.fromId===fromId&&x.toId===toId));if(active)rows.unshift({id:uid("thumb"),fromId,toId,decision:"thumb",createdAt:now()});write(KEYS.thumbs,rows.slice(0,3000));return active}
  function toggleThumb(targetId){const active=!hasThumb(myId(),targetId);setThumb(myId(),targetId,active);return{active,connected:active&&isConnection(targetId)}}
  function hasThumb(fromId,toId){return thumbs().some(x=>x.fromId===fromId&&x.toId===toId&&(x.decision==="thumb"||x.decision==="like"))}
  function isConnection(otherId){return hasThumb(myId(),otherId)&&hasThumb(otherId,myId())}
  function incomingThumbs(){return visiblePeople().filter(person=>hasThumb(person.id,myId()))}
  function connections(){return visiblePeople().filter(person=>isConnection(person.id))}
  const requests=()=>read(KEYS.requests,[]);
  function requestEndAt(item){return item.eventEndAt||eventEndAt({event_date:item.eventDate,event_time:item.eventTime,event_end_date:item.eventEndDate,event_end_time:item.eventEndTime})}
  function isRequestActive(item){const end=requestEndAt(item);return Boolean(end&&new Date(end).getTime()>Date.now())}
  function activeIncomingRequests(){return requests().filter(item=>item.type==="event"&&String(item.toId)===myId()&&isRequestActive(item)).sort((a,b)=>String(a.eventDate||"").localeCompare(String(b.eventDate||"")))}
  function sendRequest(targetId,type="event",event=null){const target=people().find(x=>x.id===targetId);if(!target)return{ok:false,message:"Профиль не найден"};if(type!=="event"||!event?.id)return{ok:false,message:"Выберите мероприятие"};const endAt=eventEndAt(event);if(!endAt||new Date(endAt).getTime()<=Date.now())return{ok:false,message:"Мероприятие уже завершено"};if(requests().some(x=>x.fromId===myId()&&x.toId===targetId&&x.type==="event"&&String(x.eventId)===String(event.id)&&isRequestActive(x)))return{ok:false,message:"Приглашение на это мероприятие уже отправлено"};const item={id:uid("req"),fromId:myId(),fromName:profile().name,toId:targetId,toName:target.name,type:"event",status:"pending",eventId:event.id,eventTitle:event.title||"Мероприятие BALI",eventDate:event.event_date||"",eventTime:event.event_time||"23:00",eventEndDate:event.event_end_date||event.end_date||"",eventEndTime:event.event_end_time||event.end_time||"06:00",eventEndAt:endAt,createdAt:now()};const rows=requests();rows.unshift(item);write(KEYS.requests,rows.slice(0,1000));return{ok:true,item}}
  function respond(id,decision){const rows=requests(),index=rows.findIndex(x=>x.id===id&&String(x.toId)===myId());if(index<0||!isRequestActive(rows[index]))return null;const status=decision===true?"accepted":decision===false?"declined":["accepted","declined","pending"].includes(decision)?decision:"pending";rows[index]={...rows[index],status,respondedAt:now()};write(KEYS.requests,rows);return rows[index]}
  const gifts=()=>read(KEYS.gifts,[]);
  const incomingGifts=()=>gifts().filter(item=>String(item.toId)===myId()).sort((a,b)=>String(b.createdAt||"").localeCompare(String(a.createdAt||"")));
  function recordGift(targetId,giftId,source="beta_demo"){const gift=GIFT_CATALOG.find(x=>x.id===giftId&&x.active!==false),target=people().find(x=>x.id===targetId);if(!gift||!target)return null;const item={id:uid("gift"),fromId:myId(),fromName:profile().name,toId:targetId,toName:target.name,giftId,giftName:gift.name,icon:gift.icon,stars:gift.stars,source,createdAt:now()};const rows=gifts();rows.unshift(item);write(KEYS.gifts,rows.slice(0,500));return item}
  function adminGift(targetId,giftId,note="Подарок от BALI"){const gift=GIFT_CATALOG.find(x=>x.id===giftId&&x.active!==false),target=people().find(x=>String(x.id)===String(targetId));if(!gift||!target)return{ok:false,message:"Пользователь или подарок не найден"};const item={id:uid("gift"),fromId:"bali-admin",fromName:"BALI Night Club",toId:String(targetId),toName:target.name,giftId,giftName:gift.name,icon:gift.icon,stars:gift.stars,pointsCost:0,currency:"admin_gift",source:"admin_gift",note:String(note||""),createdAt:now()};const rows=gifts();rows.unshift(item);write(KEYS.gifts,rows.slice(0,1000));return{ok:true,item}}
  function removeGift(id){const rows=gifts(),item=rows.find(x=>x.id===id);if(!item)return{ok:false,message:"Подарок не найден"};write(KEYS.gifts,rows.filter(x=>x.id!==id));return{ok:true,item}}
  window.addEventListener("storage",event=>{if(event.key===KEYS.giftCatalog){syncGiftCatalog();window.dispatchEvent(new CustomEvent("bali:social-changed",{detail:{key:KEYS.giftCatalog}}))}});
  window.BaliBeta4Social={KEYS,STATUSES,DEFAULT_GIFT_CATALOG,GIFT_CATALOG,saveGiftCatalog,profile,saveProfile,people,visiblePeople,thumbs,toggleThumb,setThumb,hasThumb,isConnection,incomingThumbs,connections,requests,sendRequest,respond,eventEndAt,requestEndAt,isRequestActive,activeIncomingRequests,gifts,incomingGifts,recordGift,adminGift,removeGift,myId};
})();
