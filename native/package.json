{
  "name": "bali-native-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "web:prepare": "node scripts/prepare-web.mjs",
    "ios:add": "cap add ios",
    "ios:sync": "npm run web:prepare && cap sync ios",
    "ios:open": "cap open ios",
    "android:add": "cap add android",
    "android:sync": "npm run web:prepare && cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "8.4.2",
    "@capacitor/ios": "8.4.2",
    "@capacitor/android": "8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.4.2"
  }
}
